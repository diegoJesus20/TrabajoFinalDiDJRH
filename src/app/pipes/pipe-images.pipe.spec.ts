import { PipeImagesPipe } from './pipe-images.pipe';

describe('PipeImagesPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeImagesPipe();
    expect(pipe).toBeTruthy();
  });
});
