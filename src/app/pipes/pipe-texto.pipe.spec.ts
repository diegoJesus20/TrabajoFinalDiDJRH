import { PipeTextoPipe } from './pipe-texto.pipe';

describe('PipeTextoPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeTextoPipe();
    expect(pipe).toBeTruthy();
  });
});
